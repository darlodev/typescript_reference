// VARIABLES

// variables are assigned types in TypeScript, this is done in either an explicit or implicit way

let variableOne: string = "one";
// explicit - the type is written in the declaration

let variableTwo = "two";
// implicit - the type is guessed from the assigned value